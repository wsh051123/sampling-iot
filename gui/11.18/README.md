# CS1237 ADC 控制器

基于 PyQt6 的 CS1237 ADC 数据采集与可视化工具。

## 功能特性

- 串口通信控制
- 实时数据采集与可视化
- PGA 增益配置
- 采样率调整
- 数据导出（TXT/CSV格式）
- 数据分析窗口

## 依赖

- Python >= 3.8
- PyQt6 >= 6.4.0
- pyserial >= 3.5
- matplotlib >= 3.5.0

## 使用方法

```bash
# 使用 uv 安装依赖
uv sync

# 运行程序
uv run python 11.14.py
```
